/** Prompt blocks for PR description generation (writing style, visuals, review map). */

export const descriptionBodyScaleGuidance = [
  "## Description body scale",
  "",
  "The server chooses body scale S, M, or L from workspace size stats and injects a hard rule in the user message.",
  "Follow that rule for bullet count, words per bullet, and technical depth.",
  "Do not stay at a fixed short template when the hard rule asks for more detail.",
  "Do not pad with empty or repeated bullets when the hard rule asks for fewer.",
  "",
  "### Depth meanings",
  "- what_why: state what changed and why it matters for reviewers.",
  "- what_why_risk: also name notable risks, contracts, or failure modes the diff touches.",
  "- what_why_how: also name key modules or paths and how they interact.",
].join("\n");

export const descriptionVisualsGuidance = [
  "## Visuals (visuals[])",
  "",
  "Optional flat array of publishable fenced sketches beside the description bullets.",
  "Omit visuals when the diff does not prove a shape worth sketching.",
  "Each entry needs `kind` and `content`. Use `language` only for `component_tree` or `full_block`.",
  "",
  "### Allowed kinds",
  "- pseudocode: logic or algorithm in a text fence",
  "- call_tree: runtime control flow in a text fence",
  "- component_tree: UI structure in a tsx fence",
  "- file_tree: shallow responsibility map in a text fence",
  "- mermaid: interaction or data flow in a mermaid fence",
  "- diff: delta sketch of an existing shape in a diff fence",
  "- full_block: copyable target block when the diff proves most of a new shape",
  "",
  "### Rules",
  "- Name only calls, files, props, states, and boundaries the diff proves.",
  "- Do not emit HTML fences or link untracked artifacts.",
  "- For every tier, add a visual for each theme with a proved shape when a sketch helps reviewers.",
  "- Place visuals after the description bullets in submit order; the server renders them as fences.",
  "",
  "### Mermaid syntax (GitHub must render it)",
  "- Content must be a ```mermaid fenced block.",
  "- First line inside the fence: `flowchart LR` (preferred) or `flowchart TB`.",
  '- Use simple node ids (A, B, C…). Put human text in **quoted** labels: `A["Load course"]`.',
  "- Do **not** use `/subroutine/` node shapes.",
  "- One connected graph only; for large PRs use ≤8 nodes and group steps.",
  "",
  "### Good mermaid example",
  "```mermaid",
  "flowchart LR",
  '  A["Admin form save"] --> B["Render-safe redirect"]',
  '  B --> C["Next navigation"]',
  "```",
].join("\n");

export const descriptionReviewMapGuidance = [
  "## Review map (prFiles)",
  "",
  "Server chooses map mode from workspace size stats and injects a hard rule in the user message.",
  "Follow that rule exactly.",
  "",
  "### When mode is omit",
  "- Do **not** emit `prFiles`.",
  "- Publish type, description bullets, and optional visuals only.",
  "",
  "### When mode is read_first",
  "- Emit `prFiles` with **1–5** entries only (never more).",
  "- Order by review risk: auth, data, migrations, core API before tests, docs, or chore.",
  "- Each entry needs only:",
  "  - `filename` (a changed path from the PR)",
  "  - `changesTitle` (one short clause: why open this file first)",
  "- Do **not** restate the top description bullets.",
  "- Do **not** list every changed file.",
  "- Do **not** group by PR-type labels (enhancement, bug fix, …).",
  "- Do **not** emit multi-bullet per-file summaries (`changesSummary`) or grouping `label`.",
].join("\n");
