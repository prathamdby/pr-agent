---
globs:
  - "src/**"
---

Keep public entries: review → `run/reviewRun.ts`, `publish/publishReview.ts`, `ci/analyzeCi.ts`; workspace → `prWorkspace/index.ts`; intake → `planner.ts` / `applier.ts`; executors → `executors/index.ts`; web/worker → `agentWork/runtime.ts`. Import GitHub review error helpers (`isLineResolutionPublishError`, etc.) from `src/github/reviewErrors.js` — not via `reviewDiffPlacement.ts` or other placement re-exports.
