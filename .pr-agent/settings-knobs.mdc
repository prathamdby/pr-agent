Flag new magic numbers or default strings outside `src/settings/`. Knob changes must update the matching checklist in the same PR: feature → `featureModes` / `envKeys` / `config` / `.env.example` / `docs/features.md`; env → `envKeys` / `defaults` / `config` / `.env.example` / `docs/configuration.md`; code constant → `*Constants.ts` + `docs/configuration.md`. Import constants via `src/settings/index.js`. Import `Config` from `config.ts` only at runtime entry points — never invent parallel defaults in feature modules.
